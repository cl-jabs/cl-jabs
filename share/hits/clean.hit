;; -*- mode: lisp -*-
(clean :depends-on ()
       (let ((target-dir
	      (or
	       (try (get-skeleton-target (find-skeleton (project-slot-value *jabs-current-project* 'skeleton))))
	       (try (get-skeleton-target (find-skeleton (car (project-slot-value *jabs-current-project* 'skeleton))))))))
	 (when target-dir
	   (os-rm target-dir :recursive t))))
