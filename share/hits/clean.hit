;; -*- mode: lisp -*-
(clean :depends-on ()
       (let ((target-dir
	      (or
	       (try (get-skelethon-target (find-skelethon (project-slot-value *jabs-current-project* 'skelethon))))
	       (try (get-skelethon-target (find-skelethon (car (project-slot-value *jabs-current-project* 'skelethon))))))))
	 (when target-dir
	   (os-rm target-dir :recursive t))))
