# Quickstart

```bash
$ mkdir my-project
$ cd my-project
$ jab --generate-build-jab
$ emacs build.jab ## (make your changes, if it needed)
$ jab --generate-skelethon
## load your files to project in source directory
$ jab ## build project
```
